# References

This skill uses detailed rules in `rules/` for Remotion-style video (animations, timing, captions, assets). Use `templates/` for video plan and code structure. If you need a minimal example output, see `examples/` when present.
